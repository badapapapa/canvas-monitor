# No dependency lifecycle scripts, ever (DECISIONS.md D-66). None of the
# locked dependencies declares one; this makes a future one fail safe rather
# than run. `npm run build` still runs: explicit scripts are unaffected.
ignore-scripts=true
